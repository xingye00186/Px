test(aot): C2 exit criterion CLOSED — compare_php_aot 55/56 identical on current code

The AOT verification chain is finally complete. Two things unblocked it:

1. User pointed out --skip-build exists (PipelineBuilder L79). The pipeline was
   re-running the full AOT build on every invocation, which is what had been
   burning 30+ minutes and timing out - not the tests themselves. With
   --skip-build the whole 56-case AOT pass finishes in 63.69s.

2. The css-test AOT exe (07-31 00:23, 127.7MB, built by the user) postdates the
   SelectorParser C2440 fix, so it confirms that fix compiles and links.

RESULT on current code (all _aot.json regenerated 07-31 08:17):

    compared: 56 cases | identical: 55 | diff: 1
      case-050-text-emphasis: geo=43 style=0 onlyPhp=0 onlyAot=0 (n 268 vs 268)

versus the stale 07-29 data I had been quoting, which predated every change in
this batch:

    compared: 55 | identical: 49 | diff: 7

So CLI-vs-AOT agreement went 89% -> 98.2%. case-058-rgba-alpha's MISSING(aot)
is gone, and the geo/style diffs on all four scroll cases (027/028/032) went to
zero. The single remaining case-050 diff is geometry-only (style=0) with equal
element counts on both sides (268 vs 268), i.e. not a structural divergence.

NOTE, so the run is not over-read: the pipeline's own summary printed
"Passed: 0 / 56". That is NOT 56 real layout failures - the per-case comparator
reported total=2 elements while the browser side collected 224, so the
browser-vs-engine comparison layer had almost nothing to compare. The AOT export
itself is healthy (engine_ref_level_0_aot.json is 496KB of real data, which is
exactly what compare_php_aot then consumed successfully). That comparator
shortfall is a separate defect and is NOT diagnosed here.
