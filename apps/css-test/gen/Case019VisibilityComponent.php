<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: Visibility.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case019VisibilityComponent extends ReactiveComponent
{



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'bx-card','style'=>'width:720px;box-sizing:border-box;margin:0 auto;background:#fff;border-radius:12px;padding:28px;box-shadow:0 2px 12px rgba(0,0,0,.08);position:relative'], [
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['class'=>'bx-header','style'=>'font-size:20px;font-weight:700;margin-bottom:20px;color:#1a1a2e;border-bottom:2px solid #e94560;padding-bottom:12px;'], '??? 布局 ・ Visibility vs Display 测试'),
                                    VNode::h('div', ['class'=>'bx-row','style'=>'display:flex;gap:12px;align-items:center;margin-bottom:16px;'], [
                                            VNode::h('div', ['style'=>'flex:1;padding:16px;background:#e8f5e9;border-radius:8px;text-align:center;border:1px solid #4ade80;'], 'Normal'),
                                            VNode::h('div', ['style'=>'flex:1;padding:16px;background:#fff3e0;border-radius:8px;text-align:center;border:1px solid #fb923c;visibility:hidden;','id'=>'vis-hidden'], 'visibility: hidden (占位)'),
                                            VNode::h('div', ['style'=>'flex:1;padding:16px;background:#fce4ec;border-radius:8px;text-align:center;border:1px solid #e94560;display:none;','id'=>'disp-none'], 'display: none'),
                                            VNode::h('div', ['style'=>'flex:1;padding:16px;background:#e8f5e9;border-radius:8px;text-align:center;border:1px solid #4ade80;'], 'Normal'),
                                        ]),
                                    VNode::h('div', ['class'=>'bx-desc','style'=>'font-size:13px;color:#888;line-height:1.6;background:#fafafa;padding:12px;border-radius:6px;'], [
                                            VNode::h('code', ['style'=>'background:#f0f0f0;padding:2px 6px;border-radius:3px;'], 'visibility:hidden'),
                                            '隐藏但保留空间(占位)；',
                                            VNode::h('code', [], 'display:none'),
                                            '完全消失不占空间。',
                                            VNode::h('br', []),
                                            '所以 Normal 之间应只有 1 个间隙(visibility占位)而非 2 个。',
                                        ]),
                                    VNode::h('div', ['class'=>'bx-footer','style'=>'margin-top:16px;padding-top:14px;border-top:1px solid #eee;font-size:12px;color:#aaa;text-align:center;'], 'case-019: Layout ― Visibility vs Display'),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        // No bind keys defined
    }

    public function getBindValue(string $bindKey): string
    {
        return '';
    }


    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'Visibility');
    }
}