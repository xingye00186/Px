<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: MinMaxHeight.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case015MinMaxHeightComponent extends ReactiveComponent
{



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'bx-card','style'=>'width:720px;box-sizing:border-box;margin:0 auto;background:#fff;border-radius:12px;padding:28px;box-shadow:0 2px 12px rgba(0,0,0,.08);position:relative'], [
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['class'=>'bx-header','style'=>'font-size:20px;font-weight:700;margin-bottom:20px;color:#1a1a2e;border-bottom:2px solid #e94560;padding-bottom:12px;'], '?? 布局 ・ Min/Max 约束测试'),
                                    VNode::h('div', ['class'=>'bx-row','style'=>'display:flex;gap:16px;align-items:stretch;'], [
                                            VNode::h('div', ['class'=>'mm-item','style'=>'flex:1;padding:16px;background:#f8f9ff;border-radius:8px;border:1px solid #e0e0e0;'], [
                                                    VNode::h('div', ['style'=>'font-size:12px;color:#999;margin-bottom:8px;'], 'min-height: 100px'),
                                                    VNode::h('div', ['class'=>'mm-min','style'=>'min-height:100px;background:linear-gradient(135deg,#667eea,#764ba2);border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:12px;color:#fff;padding:10px;'], 'min-h 生效'),
                                                ]),
                                            VNode::h('div', ['class'=>'mm-item','style'=>'flex:1;padding:16px;background:#f8f9ff;border-radius:8px;border:1px solid #e0e0e0;'], [
                                                    VNode::h('div', ['style'=>'font-size:12px;color:#999;margin-bottom:8px;'], 'max-height: 80px (内容多)'),
                                                    VNode::h('div', ['class'=>'mm-max','style'=>'max-height:80px;background:linear-gradient(135deg,#f093fb,#f5576c);border-radius:6px;font-size:12px;color:#fff;padding:10px;overflow:hidden;'], [
                                                            '多行文本内容',
                                                            VNode::h('br', []),
                                                            '第2行',
                                                            VNode::h('br', []),
                                                            '第3行',
                                                            VNode::h('br', []),
                                                            '第4行',
                                                            VNode::h('br', []),
                                                            '第5行',
                                                            VNode::h('br', []),
                                                            '第6行',
                                                            VNode::h('br', []),
                                                            '第7行',
                                                        ]),
                                                ]),
                                            VNode::h('div', ['class'=>'mm-item','style'=>'flex:1;padding:16px;background:#f8f9ff;border-radius:8px;border:1px solid #e0e0e0;'], [
                                                    VNode::h('div', ['style'=>'font-size:12px;color:#999;margin-bottom:8px;'], '无约束 (参考)'),
                                                    VNode::h('div', ['style'=>'background:linear-gradient(135deg,#4ade80,#38f9d7);border-radius:6px;font-size:12px;color:#333;padding:10px;'], '自适应高度'),
                                                ]),
                                        ]),
                                    VNode::h('div', ['class'=>'bx-footer','style'=>'margin-top:16px;padding-top:14px;border-top:1px solid #eee;font-size:12px;color:#aaa;text-align:center;'], 'case-015: Layout ― Min / Max Height'),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        // No bind keys defined
    }

    public function getBindValue(string $bindKey): string
    {
        return '';
    }


    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'MinMaxHeight');
    }
}