<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: ScrollGrid.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case031ScrollGridComponent extends ReactiveComponent
{

    public array $items = [];
    public string $count = '0';

    public function onMount(): void
    {
        $colors = ['#f3e5f5', '#ffffff', '#f5f5f5', '#ede7f6'];
        $items = [];
        for ($i = 1; $i <= 25; $i++) {
            $items[] = ['id' => (string)$i, 'bg' => $colors[$i % count($colors)]];
        }
        $this->items = $items;
        $this->count = '25';
        $this->markDirty();
    }



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'card','style'=>'width:800px;margin:0 auto;box-sizing:border-box;background:#fff;border-radius:12px;padding:24px;border:1px solid #e0e0e0;position:relative;box-shadow:0 2px 12px rgba(0,0,0,.08);'], [
                                    VNode::h('div', ['style'=>'margin-bottom:10px;font-size:18px;font-weight:700;color:#6a1b9a;'], 'Scroll Grid'),
                                    VNode::h('div', ['style'=>'margin-bottom:14px;font-size:12px;color:#555;line-height:1.6;border-left:3px solid #6a1b9a;padding:10px 14px;background:#f3e5f5;border-radius:4px;'], [
                                            VNode::h('b', [], 'Layout:'),
                                            'Grid container with 3 columns. The main content area (col 2) has overflow-y:auto.',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Container:'),
                                            'Grid 700px x 350px. Scroll area in middle column ~420px wide x 350px tall.',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Items:'),
                                            '25 items x 36px = 900px content.',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Expected:'),
                                            'Content in grid cell scrolls correctly. Other grid cells stay fixed.',
                                        ]),
                                    VNode::h('div', ['class'=>'grid-host','style'=>'width:100%;height:350px;display:grid;grid-template-columns:120px 1fr 120px;border:2px solid #6a1b9a;border-radius:6px;background:#fafafa;'], [
                                            VNode::h('div', ['style'=>'display:flex;align-items:center;justify-content:center;background:#f3e5f5;border-right:1px solid #6a1b9a;font-size:12px;color:#6a1b9a;padding:8px;'], 'Sidebar Left'),
                                            VNode::h('div', ['class'=>'scroll-area','style'=>'overflow-y:auto;padding:4px 0;background:#fafafa;'], $this->render_0()),
                                            VNode::h('div', ['style'=>'display:flex;align-items:center;justify-content:center;background:#f3e5f5;border-left:1px solid #6a1b9a;font-size:12px;color:#6a1b9a;padding:8px;'], 'Sidebar Right'),
                                        ]),
                                    VNode::h('div', ['style'=>'margin-top:14px;font-size:11px;color:#aaa;text-align:center;border-top:1px solid #eee;padding-top:10px;'], 'case-031-scroll-grid &middot; ' . $this->count . ' items &middot; display:grid + overflow-y:auto in grid cell'),
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        switch ($bindKey) {
            case 'count': if ($this->count !== $value) { $this->count = $value; $this->markDirty(); } break;
            default: break;
        }
    }

    public function getBindValue(string $bindKey): string
    {
        switch ($bindKey) {
            case 'count': return $this->count;
            default: return '';
        }
    }

    /**
     * v-for render helper: item in items
     * @return VNode[]
     */
    private function render_0(): array
    {
        $children = [];
        foreach ($this->items as $item) {
            $children[] = VNode::h('div', ['style'=>'height:36px;display:flex;align-items:center;padding:0 12px;font-size:13px;border-bottom:1px solid #e0e0e0;',':style'=>'background:' . $item['bg']], [
                    VNode::h('span', ['style'=>'display:inline-block;width:32px;font-weight:700;color:#6a1b9a;','parts'=>array (
  0 => 
  array (
    'type' => 'bind',
    'expr' => 'item.id',
  ),
  1 => 
  array (
    'type' => 'text',
    'value' => '.',
  ),
)], $item['id'] . '.'),
                    VNode::h('span', ['style'=>'color:#37474f;','parts'=>array (
  0 => 
  array (
    'type' => 'text',
    'value' => 'Grid Item #',
  ),
  1 => 
  array (
    'type' => 'bind',
    'expr' => 'item.id',
  ),
)], 'Grid Item #' . $item['id']),
                    VNode::h('span', ['style'=>'margin-left:10px;font-size:11px;color:#aaa;'], '36px')
                ]);
        }
        return $children;
    }

    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'ScrollGrid');
    }
}