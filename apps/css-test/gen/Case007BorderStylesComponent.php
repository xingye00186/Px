<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: BorderStyles.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case007BorderStylesComponent extends ReactiveComponent
{



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'bx-card','style'=>'width:720px;box-sizing:border-box;margin:0 auto;background:#fff;border-radius:12px;padding:28px;position:relative'], [
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['class'=>'bx-header','style'=>'font-size:20px;font-weight:700;margin-bottom:20px;color:#1a1a2e;border-bottom:2px solid #e94560;padding-bottom:12px;'], '?? 盒模型 ・ 边框样式测试'),
                                    VNode::h('div', ['class'=>'bx-desc','style'=>'font-size:13px;color:#888;margin-bottom:20px;line-height:1.5;'], '每侧不同颜色+宽度：边框红(4px) 上 / 蓝(3px) 右 / 绿(2px) 下 / 橙(1px) 左'),
                                    VNode::h('div', ['style'=>'width:300px;height:120px;box-sizing:border-box;margin:0 auto 24px;border-top:4px solid #e94560;border-right:3px solid #0f3460;border-bottom:2px solid #4ade80;border-left:1px solid #fb923c;border-radius:10px;background:#f8f9ff;display:flex;align-items:center;justify-content:center;font-size:14px;color:#444;','id'=>'target-box'], '不同方向边框'),
                                    VNode::h('div', ['class'=>'bx-grid','style'=>'display:flex;gap:16px;flex-wrap:wrap;justify-content:center;'], [
                                            VNode::h('div', ['class'=>'bx-item','style'=>'flex:1;min-width:140px;padding:16px;background:#f8f9ff;border-radius:8px;text-align:center;'], [
                                                    VNode::h('div', ['style'=>'font-size:11px;color:#999;'], '边框圆角'),
                                                    VNode::h('div', ['style'=>'width:80px;height:60px;margin:10px auto 0;border:2px solid #0f3460;border-radius:16px 4px 16px 4px;background:#0f3460;','id'=>'box-radius-test']),
                                                    VNode::h('div', ['style'=>'font-size:11px;margin-top:8px;color:#666;'], 'border-radius: 16px 4px'),
                                                ]),
                                            VNode::h('div', ['class'=>'bx-item','style'=>'flex:1;min-width:140px;padding:16px;background:#f8f9ff;border-radius:8px;text-align:center;'], [
                                                    VNode::h('div', ['style'=>'font-size:11px;color:#999;'], '虚线边框'),
                                                    VNode::h('div', ['style'=>'width:80px;height:60px;margin:10px auto 0;border:3px solid #e94560;border-radius:8px;background:#fff;']),
                                                    VNode::h('div', ['style'=>'font-size:11px;margin-top:8px;color:#666;'], 'border: 3px solid'),
                                                ]),
                                            VNode::h('div', ['class'=>'bx-item','style'=>'flex:1;min-width:140px;padding:16px;background:#f8f9ff;border-radius:8px;text-align:center;'], [
                                                    VNode::h('div', ['style'=>'font-size:11px;color:#999;'], '无边框'),
                                                    VNode::h('div', ['style'=>'width:80px;height:60px;margin:10px auto 0;background:#f0fdf4;border-radius:8px;']),
                                                    VNode::h('div', ['style'=>'font-size:11px;margin-top:8px;color:#666;'], 'border: none'),
                                                ]),
                                        ]),
                                    VNode::h('div', ['class'=>'bx-footer','style'=>'margin-top:20px;padding-top:14px;border-top:1px solid #eee;font-size:12px;color:#aaa;text-align:center;'], 'case-007: Box Model ― Border Styles'),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        // No bind keys defined
    }

    public function getBindValue(string $bindKey): string
    {
        return '';
    }


    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'BorderStyles');
    }
}