<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: OverflowHidden.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case014OverflowHiddenComponent extends ReactiveComponent
{



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'bx-card','style'=>'width:720px;box-sizing:border-box;margin:0 auto;background:#fff;border-radius:12px;padding:28px;box-shadow:0 2px 12px rgba(0,0,0,.08);position:relative'], [
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['class'=>'bx-header','style'=>'font-size:20px;font-weight:700;margin-bottom:20px;color:#1a1a2e;border-bottom:2px solid #e94560;padding-bottom:12px;'], '?? 布局 ・ Overflow Hidden 裁切测试'),
                                    VNode::h('div', ['class'=>'bx-desc','style'=>'font-size:13px;color:#888;margin-bottom:16px;line-height:1.5;'], '容器 overflow:hidden，宽250高120，超出的绿色块应被裁切'),
                                    VNode::h('div', ['class'=>'oh-container','style'=>'width:250px;height:120px;overflow:hidden;border:2px solid #0f3460;border-radius:8px;margin:0 auto 16px;position:relative;background:#f8f9ff;'], [
                                            VNode::h('div', ['class'=>'oh-content','style'=>'width:300px;height:80px;background:linear-gradient(90deg,#4ade80,#38f9d7);border-radius:4px;display:flex;align-items:center;justify-content:center;font-size:13px;color:#333;position:relative;top:20px;'], '宽度300 > 容器250'),
                                            VNode::h('div', ['class'=>'oh-over','style'=>'width:200px;height:50px;background:#e94560;border-radius:4px;display:flex;align-items:center;justify-content:center;font-size:12px;color:#fff;margin-top:10px;'], '还在可视区'),
                                        ]),
                                    VNode::h('div', ['class'=>'bx-desc','style'=>'font-size:13px;color:#888;line-height:1.6;background:#fafafa;padding:12px;border-radius:6px;'], '? overflow:hidden 应对超出部分进行裁切，不显示滚动条。'),
                                    VNode::h('div', ['class'=>'bx-footer','style'=>'margin-top:16px;padding-top:14px;border-top:1px solid #eee;font-size:12px;color:#aaa;text-align:center;'], 'case-014: Layout ― Overflow Hidden'),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        // No bind keys defined
    }

    public function getBindValue(string $bindKey): string
    {
        return '';
    }


    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'OverflowHidden');
    }
}