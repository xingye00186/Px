<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: WhiteSpace.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case022WhiteSpaceComponent extends ReactiveComponent
{



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'bx-card','style'=>'width:720px;box-sizing:border-box;margin:0 auto;background:#fff;border-radius:12px;padding:28px;box-shadow:0 2px 12px rgba(0,0,0,.08);position:relative'], [
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['class'=>'bx-header','style'=>'font-size:20px;font-weight:700;margin-bottom:20px;color:#1a1a2e;border-bottom:2px solid #e94560;padding-bottom:12px;'], '?? 文本 ・ 空白/溢出测试'),
                                    VNode::h('div', ['class'=>'ws-section','style'=>'margin-bottom:12px;padding:14px;background:#f8f9ff;border-radius:8px;border:1px solid #e0e0e0;'], [
                                            VNode::h('div', ['style'=>'font-size:11px;color:#999;margin-bottom:4px;'], 'white-space: nowrap + text-overflow: ellipsis'),
                                            VNode::h('div', ['class'=>'ws-ellipsis','style'=>'width:300px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;font-size:14px;color:#444;padding:8px;background:#fff;border-radius:4px;border:1px solid #eee;'], '这是一段非常长的文本内容，超长时应该显示省略号而不是换行显示'),
                                        ]),
                                    VNode::h('div', ['class'=>'ws-section','style'=>'margin-bottom:16px;padding:14px;background:#f8f9ff;border-radius:8px;border:1px solid #e0e0e0;'], [
                                            VNode::h('div', ['style'=>'font-size:11px;color:#999;margin-bottom:4px;'], 'white-space: pre (保留空格和换行)'),
                                            VNode::h('div', ['class'=>'ws-pre','style'=>'white-space:pre;font-size:14px;color:#444;padding:8px;background:#fff;border-radius:4px;border:1px solid #eee;font-family:monospace;'], '保留    空格
        保留换行
        缩进效果'),
                                        ]),
                                    VNode::h('div', ['class'=>'bx-footer','style'=>'margin-top:16px;padding-top:14px;border-top:1px solid #eee;font-size:12px;color:#aaa;text-align:center;'], 'case-022: Text ― White Space / Overflow'),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        // No bind keys defined
    }

    public function getBindValue(string $bindKey): string
    {
        return '';
    }


    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'WhiteSpace');
    }
}