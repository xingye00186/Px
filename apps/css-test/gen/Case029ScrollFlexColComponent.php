<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: ScrollFlexCol.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case029ScrollFlexColComponent extends ReactiveComponent
{

    public array $items = [];
    public string $count = '0';

    public function onMount(): void
    {
        $colors = ['#fff3e0', '#ffffff', '#f5f5f5', '#fbe9e7', '#fff8e1'];
        $items = [];
        for ($i = 1; $i <= 30; $i++) {
            $items[] = ['id' => (string)$i, 'bg' => $colors[$i % count($colors)]];
        }
        $this->items = $items;
        $this->count = '30';
        $this->markDirty();
    }



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'card','style'=>'width:800px;margin:0 auto;box-sizing:border-box;background:#fff;border-radius:12px;padding:24px;border:1px solid #e0e0e0;position:relative;box-shadow:0 2px 12px rgba(0,0,0,.08);'], [
                                    VNode::h('div', ['style'=>'margin-bottom:10px;font-size:18px;font-weight:700;color:#e65100;'], 'Scroll Flex Column'),
                                    VNode::h('div', ['style'=>'margin-bottom:14px;font-size:12px;color:#555;line-height:1.6;border-left:3px solid #e65100;padding:10px 14px;background:#fff3e0;border-radius:4px;'], [
                                            VNode::h('b', [], 'Layout:'),
                                            'Flex container (column) → flex:1 child with overflow-y:auto.',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'This matches the .case-list scenario — the exact bug pattern.'),
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Container:'),
                                            'flex container 250px, header+footer 52px each, flex:1 child fills remainder (~146px).',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Items:'),
                                            '30 items x 36px = 1080px content.',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Expected:'),
                                            'Wheel + drag scroll stop at bottom. No bounce-back on drag release.',
                                        ]),
                                    VNode::h('div', ['class'=>'flex-host','style'=>'height:250px;display:flex;flex-direction:column;border:2px solid #e65100;border-radius:6px;background:#fafafa;'], [
                                            VNode::h('div', ['style'=>'height:52px;flex-shrink:0;display:flex;align-items:center;padding:0 16px;background:#fff3e0;border-bottom:1px solid #e65100;font-size:13px;font-weight:600;color:#e65100;'], 'Flex Header (52px fixed)'),
                                            VNode::h('div', ['class'=>'scroll-area','style'=>'flex:1;overflow-y:auto;min-height:0;padding:4px 0;background:#fafafa;'], $this->render_0()),
                                            VNode::h('div', ['style'=>'height:52px;flex-shrink:0;display:flex;align-items:center;padding:0 16px;background:#fff3e0;border-top:1px solid #e65100;font-size:13px;font-weight:600;color:#e65100;'], 'Flex Footer (52px fixed)'),
                                        ]),
                                    VNode::h('div', ['style'=>'margin-top:14px;font-size:11px;color:#aaa;text-align:center;border-top:1px solid #eee;padding-top:10px;'], 'case-029-scroll-flex-col &middot; ' . $this->count . ' items &middot; flex:1 + overflow-y:auto'),
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        switch ($bindKey) {
            case 'count': if ($this->count !== $value) { $this->count = $value; $this->markDirty(); } break;
            default: break;
        }
    }

    public function getBindValue(string $bindKey): string
    {
        switch ($bindKey) {
            case 'count': return $this->count;
            default: return '';
        }
    }

    /**
     * v-for render helper: item in items
     * @return VNode[]
     */
    private function render_0(): array
    {
        $children = [];
        foreach ($this->items as $item) {
            $children[] = VNode::h('div', ['style'=>'height:36px;display:flex;align-items:center;padding:0 14px;font-size:13px;border-bottom:1px solid #e0e0e0;',':style'=>'background:' . $item['bg']], [
                    VNode::h('span', ['style'=>'display:inline-block;width:36px;font-weight:700;color:#e65100;','parts'=>array (
  0 => 
  array (
    'type' => 'bind',
    'expr' => 'item.id',
  ),
  1 => 
  array (
    'type' => 'text',
    'value' => '.',
  ),
)], $item['id'] . '.'),
                    VNode::h('span', ['style'=>'color:#37474f;','parts'=>array (
  0 => 
  array (
    'type' => 'text',
    'value' => 'FlexCol Item #',
  ),
  1 => 
  array (
    'type' => 'bind',
    'expr' => 'item.id',
  ),
)], 'FlexCol Item #' . $item['id']),
                    VNode::h('span', ['style'=>'margin-left:10px;font-size:11px;color:#aaa;'], '36px')
                ]);
        }
        return $children;
    }

    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'ScrollFlexCol');
    }
}