<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: FontWeight.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case024FontWeightComponent extends ReactiveComponent
{



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'bx-card','style'=>'width:720px;box-sizing:border-box;margin:0 auto;background:#fff;border-radius:12px;padding:28px;box-shadow:0 2px 12px rgba(0,0,0,.08);position:relative'], [
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['class'=>'bx-header','style'=>'font-size:20px;font-weight:700;margin-bottom:20px;color:#1a1a2e;border-bottom:2px solid #e94560;padding-bottom:12px;'], '?? 文本 ・ 字重测试'),
                                    VNode::h('div', ['class'=>'fw-section','style'=>'margin-bottom:12px;padding:16px;background:#f8f9ff;border-radius:8px;border:1px solid #e0e0e0;'], [
                                            VNode::h('div', ['style'=>'font-size:11px;color:#999;margin-bottom:8px;'], 'font-weight 对比'),
                                            VNode::h('div', ['style'=>'display:flex;gap:24px;align-items:center;'], [
                                                    VNode::h('div', [], VNode::h('span', ['style'=>'font-weight:100;font-size:16px;'], '100 Thin')),
                                                    VNode::h('div', [], VNode::h('span', ['style'=>'font-weight:300;font-size:16px;'], '300 Light')),
                                                    VNode::h('div', [], VNode::h('span', ['style'=>'font-weight:400;font-size:16px;'], '400 Regular')),
                                                    VNode::h('div', [], VNode::h('span', ['style'=>'font-weight:500;font-size:16px;'], '500 Medium')),
                                                    VNode::h('div', [], VNode::h('span', ['style'=>'font-weight:700;font-size:16px;color:#0f3460;'], '700 Bold')),
                                                    VNode::h('div', [], VNode::h('span', ['style'=>'font-weight:900;font-size:16px;color:#e94560;'], '900 Black')),
                                                ]),
                                        ]),
                                    VNode::h('div', ['class'=>'fw-compare','style'=>'display:flex;gap:16px;'], [
                                            VNode::h('div', ['style'=>'flex:1;padding:16px;background:#f8f9ff;border-radius:8px;border:1px solid #e0e0e0;text-align:center;'], [
                                                    VNode::h('div', ['style'=>'font-size:11px;color:#999;margin-bottom:4px;'], 'normal'),
                                                    VNode::h('div', ['class'=>'fw-normal','style'=>'font-weight:400;font-size:18px;'], 'Normal'),
                                                ]),
                                            VNode::h('div', ['style'=>'flex:1;padding:16px;background:#fff7ed;border-radius:8px;border:1px solid #fb923c;text-align:center;'], [
                                                    VNode::h('div', ['style'=>'font-size:11px;color:#999;margin-bottom:4px;'], 'bold'),
                                                    VNode::h('div', ['class'=>'fw-bold','style'=>'font-weight:700;font-size:18px;color:#0f3460;'], 'Bold'),
                                                ]),
                                        ]),
                                    VNode::h('div', ['class'=>'bx-footer','style'=>'margin-top:16px;padding-top:14px;border-top:1px solid #eee;font-size:12px;color:#aaa;text-align:center;'], 'case-024: Text ― Font Weight'),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        // No bind keys defined
    }

    public function getBindValue(string $bindKey): string
    {
        return '';
    }


    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'FontWeight');
    }
}