<?php

use native_types;

/**
 * AUTO-GENERATED by SFC Compiler v9 — DO NOT EDIT
 * Source: ScrollFlexRow.vue
 */

use Px\ReactiveComponent;
use Px\Rendering\VNode;

class Case030ScrollFlexRowComponent extends ReactiveComponent
{

    public array $items = [];
    public string $count = '0';

    public function onMount(): void
    {
        $colors = ['#e8f5e9', '#ffffff', '#f1f8e9', '#e0f2f1'];
        $items = [];
        for ($i = 1; $i <= 15; $i++) {
            $items[] = ['id' => (string)$i, 'bg' => $colors[$i % count($colors)]];
        }
        $this->items = $items;
        $this->count = '15';
        $this->markDirty();
    }



    public function render(): VNode
    {
        return VNode::h('#root', ['title'=>'Untitled','style'=>''], VNode::h('div', ['style'=>'padding:20px;'], VNode::h('div', ['class'=>'card','style'=>'width:800px;margin:0 auto;box-sizing:border-box;background:#fff;border-radius:12px;padding:24px;border:1px solid #e0e0e0;position:relative;box-shadow:0 2px 12px rgba(0,0,0,.08);'], [
                                    VNode::h('div', ['style'=>'margin-bottom:10px;font-size:18px;font-weight:700;color:#2e7d32;'], 'Scroll Flex Row (horizontal)'),
                                    VNode::h('div', ['style'=>'margin-bottom:14px;font-size:12px;color:#555;line-height:1.6;border-left:3px solid #2e7d32;padding:10px 14px;background:#e8f5e9;border-radius:4px;'], [
                                            VNode::h('b', [], 'Layout:'),
                                            'Flex container (row) → flex:1 child with overflow-x:auto.',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Container:'),
                                            'flex container 700px wide, side panels 60px each, flex:1 child fills remainder (~580px).',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Items:'),
                                            '15 items x 100px wide = 1500px content (horizontal scroll).',
                                            VNode::h('br', []),
                                            VNode::h('b', [], 'Expected:'),
                                            'Horizontal wheel/drag scroll stops at right edge. scrollLeft clamped correctly.',
                                        ]),
                                    VNode::h('div', ['class'=>'flex-host','style'=>'width:100%;height:200px;display:flex;flex-direction:row;border:2px solid #2e7d32;border-radius:6px;background:#fafafa;'], [
                                            VNode::h('div', ['style'=>'width:60px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#e8f5e9;border-right:1px solid #2e7d32;font-size:12px;color:#2e7d32;writing-mode:vertical-lr;'], 'Left'),
                                            VNode::h('div', ['class'=>'scroll-area','style'=>'flex:1;overflow-x:auto;min-width:0;padding:60px 0 0 0;background:#fafafa;'], VNode::h('div', ['style'=>'display:flex;flex-direction:row;gap:8px;padding:0 8px;'], $this->render_0())),
                                            VNode::h('div', ['style'=>'width:60px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#e8f5e9;border-left:1px solid #2e7d32;font-size:12px;color:#2e7d32;writing-mode:vertical-lr;'], 'Right'),
                                        ]),
                                    VNode::h('div', ['style'=>'margin-top:14px;font-size:11px;color:#aaa;text-align:center;border-top:1px solid #eee;padding-top:10px;'], 'case-030-scroll-flex-row &middot; ' . $this->count . ' items &middot; flex:1 + overflow-x:auto (horizontal)'),
                                    VNode::h('div', ['style'=>'position:absolute;top:0;left:0;width:8px;height:8px;background:#FF00FF;pointer-events:none;']),
                                    VNode::h('div', ['style'=>'position:absolute;bottom:0;right:0;width:8px;height:8px;background:#00FFFF;pointer-events:none;']),
                                ])));
    }

    public function dispatchClick(string $handler, ?string $arg = null): void
    {
        // No event handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchClick($handler, $arg);
        }
    }

    public function dispatchKey(string $handler, string $action, int $keyCode, string $char): void
    {
        // No keyboard handlers defined — bubbling to parent
        if ($this->parent !== null) {
            $this->parent->dispatchKey($handler, $action, $keyCode, $char);
        }
    }

    public function setBindValue(string $bindKey, string $value): void
    {
        switch ($bindKey) {
            case 'count': if ($this->count !== $value) { $this->count = $value; $this->markDirty(); } break;
            default: break;
        }
    }

    public function getBindValue(string $bindKey): string
    {
        switch ($bindKey) {
            case 'count': return $this->count;
            default: return '';
        }
    }

    /**
     * v-for render helper: item in items
     * @return VNode[]
     */
    private function render_0(): array
    {
        $children = [];
        foreach ($this->items as $item) {
            $children[] = VNode::h('div', ['style'=>'width:100px;height:80px;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:13px;border-radius:6px;border:1px solid #c8e6c9;',':style'=>'background:' . $item['bg']], [
                    VNode::h('span', ['style'=>'font-weight:700;color:#2e7d32;','parts'=>array (
  0 => 
  array (
    'type' => 'text',
    'value' => '#',
  ),
  1 => 
  array (
    'type' => 'bind',
    'expr' => 'item.id',
  ),
)], '#' . $item['id'])
                ]);
        }
        return $children;
    }

    /**
     * 解析动态组件: kebab-case → PascalCase 类名
     * e.g. "case-011-position-absolute" → "Case011PositionAbsoluteComponent"
     */
    public function resolveComponent(string $tag): string
    {
        $parts = explode('-', $tag);
        $className = '';
        foreach ($parts as $p) {
            $className .= ucfirst($p);
        }
        return $className . 'Component';
    }

    /**
     * 返回编译后的 CSS class styles（从 <style> 块编译）。
     * 由 ThemeProvider::registerClassStyles() 在 mount 时读取并注册。
     */
    public function getClassStyles(): array
    {
        return [];
    }

    public function __construct(?string $componentId = null)
    {
        parent::__construct($componentId ?? 'ScrollFlexRow');
    }
}